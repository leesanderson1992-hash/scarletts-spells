// @ts-nocheck -- guarded database import script uses untyped pg rows.
import { createHash, randomUUID } from "node:crypto";
import { readFile } from "node:fs/promises";
import { resolve } from "node:path";
import pg from "pg";

const ROOT = resolve(import.meta.dirname, "..");
const PROFILE_KEY = "D4_MOR_PREFIXES_SUB_INTER_SUPER";
const PROJECT_REF = "jlhotktspjvffslvuyfz";
const FOLDER = "docs/implementation/seed-data/teaching-dictionary/candidates/2026-07-22-d4-dynamic-prefix-staging-enrichment";
const BASE_PATH = resolve(ROOT, FOLDER, "reviewed-staging-package.json");
const PACKAGE_PATH = resolve(ROOT, FOLDER, "sub-inter-super-staging-correction-package.json");
const arg=(name:string)=>{const index=process.argv.indexOf(name);return index<0?undefined:process.argv[index+1]};
const hash=(value:string)=>createHash("sha256").update(value).digest("hex");
const tokenAt=(sentence:string,index:number)=>sentence.trim().split(/\s+/).map((token)=>token.replace(/^\p{P}+|\p{P}+$/gu,"").toLowerCase())[index];
const fail=(message:string):never=>{throw new Error(message)};

async function load(){const [baseRaw,raw]=await Promise.all([readFile(BASE_PATH,"utf8"),readFile(PACKAGE_PATH,"utf8")]);const base=JSON.parse(baseRaw),correction=JSON.parse(raw);const blockers:string[]=[];if(correction.packageKey!=="adle_d4_dynamic_prefix_sub_inter_super_staging_correction_2026_07_23"||correction.profileKey!==PROFILE_KEY||correction.environment!=="staging"||correction.targetProjectRef!==PROJECT_REF||hash(baseRaw)!==correction.basePackage?.sha256)blockers.push("Invalid correction package identity or base hash.");if(Object.values(correction.activation??{}).some(Boolean))blockers.push("Correction requests prohibited writes.");const words=base.words.filter((word:any)=>word.microSkillKey===PROFILE_KEY);if(words.length!==7||correction.words?.length!==7||correction.words.some((word:string)=>!words.some((candidate:any)=>candidate.word===word)))blockers.push("Expected seven reviewed SUB/INTER/SUPER words.");const profile=correction.profile;if(profile?.introContent?.title!=="Meet the sub-, inter- and super- prefix family"||profile.introContent?.examples?.length!==3||JSON.stringify(profile.meaningBins)!==JSON.stringify([{id:"under",label:"Under",description:""},{id:"between",label:"Between",description:""},{id:"above_beyond",label:"Above or beyond",description:""}])||profile.prefixChoices?.join(",")!=="sub,inter,super")blockers.push("Approved profile examples, bins or choices are incomplete.");for(const word of words){const prefix=word.teaching?.splitParts?.filter((part:any)=>part.kind==="prefix")??[];if(!word.canonical?.frequencyBand||!word.canonical?.ageBand||!word.canonical?.complexityBand||!word.trueMorphology?.humanApprovedText||!word.teaching?.baseMeaning||!word.teaching?.childFriendlyMeaning||prefix.length!==1||word.teaching.cleaverBoundary!==prefix[0].displayRange?.end||word.teaching.splitParts.map((part:any)=>part.surfaceText).join("")!==word.word||word.dictation?.sentence!==word.dictation?.audioText||tokenAt(word.dictation?.sentence??"",word.dictation?.targetTokenIndex)!==word.word||!word.pronunciation?.ipa||!word.complexityPreview?.inputComplete)blockers.push(`${word.word}: incomplete reviewed contract`)}return{correction,raw,sha:hash(raw),blockers}}
async function apply(input:any,databaseUrl:string){const target=new URL(databaseUrl);if(!target.hostname.includes(PROJECT_REF)&&!target.username.includes(PROJECT_REF))fail("Database URL does not target staging.");const client=new pg.Client({connectionString:databaseUrl,ssl:{rejectUnauthorized:false}});await client.connect();const batchId=randomUUID();try{await client.query("begin");const before=await client.query("select (select count(*) from adle_learning_items) learning_items,(select count(*) from daily_assignments) assignments,(select count(*) from adle_assignment_attempt_events) evidence,(select count(*) from adle_review_schedule_words) scheduling");const row=await client.query("select id,production_enabled from canonical_teaching_dictionary_prefix_profiles where micro_skill_key=$1 and row_status='active' and review_status='approved_for_first_exposure' for update",[PROFILE_KEY]);if(row.rowCount!==1||row.rows[0].production_enabled)fail("Expected exactly one disabled approved staging profile.");const profileId=row.rows[0].id;const members=await client.query("select m.canonical_word_id,w.normalised_word,w.frequency_band,w.age_band,w.complexity_band,md.morphemes,md.morphology_notes,ds.dictation_sentence,ds.audio_text,ds.dictation_target_token_index,m.teaching_split_parts,m.prefix_variant,m.base_word,m.base_meaning,m.child_friendly_meaning,m.meaning_bin_key from canonical_teaching_dictionary_prefix_members m join canonical_teaching_dictionary_words w on w.id=m.canonical_word_id join canonical_teaching_dictionary_word_metadata md on md.canonical_word_id=w.id and md.row_status='active' and md.review_status='approved_for_first_exposure' join canonical_teaching_dictionary_dictation_sentences ds on ds.canonical_word_id=w.id and ds.row_status='active' and ds.review_status='approved_for_first_exposure' where m.prefix_profile_id=$1 and m.row_status='active' and m.review_status='approved_for_first_exposure' for update",[profileId]);if(members.rowCount!==7)fail("Expected seven complete members.");for(const member of members.rows){const prefix=member.teaching_split_parts?.filter((part:any)=>part.kind==="prefix")??[];if(!member.frequency_band||!member.age_band||!member.complexity_band||!member.morphemes||member.morphology_notes===null||!member.base_word||!member.base_meaning||!member.child_friendly_meaning||!member.meaning_bin_key||member.dictation_sentence!==member.audio_text||tokenAt(member.dictation_sentence,member.dictation_target_token_index)!==member.normalised_word||prefix.length!==1||member.teaching_split_parts.map((part:any)=>part.surfaceText).join("")!==member.normalised_word||member.prefix_variant!==prefix[0].surfaceText)fail(`${member.normalised_word}: dictionary invariant failed.`)}const source={correction_package_sha256:input.sha,profile_key:PROFILE_KEY,prohibited_writes:{production:0,learner:0,assignment:0,evidence:0,scheduling:0}};await client.query("insert into canonical_teaching_dictionary_import_batches (id,source_folder_path,source_folder_sha256,validator_version,validation_summary,row_counts,readiness_summary,import_mode,batch_status,source_metadata,imported_by,imported_at) values ($1,$2,$3,$4,$5,$6,$7,'admin_import','validated',$8,$9,now())",[batchId,FOLDER,input.sha,"adle_dynamic_prefix_sub_inter_super_staging_correction_v1",{errors:0},{canonical_words_updated:0,metadata_updated:0,profiles_updated:1,members_updated:0},{production_enabled:false,prohibited_writes:0},source,"ADLE guarded SUB/INTER/SUPER staging correction"]);const choices=[...input.correction.profile.prefixChoices,""] .map((text:string)=>({text,label:text?`${text}-`:"no prefix",outcome:null,meaning:null,status:"target"}));const update=await client.query("update canonical_teaching_dictionary_prefix_profiles set import_batch_id=$1,intro_content=$2::jsonb,meaning_bins=$3::jsonb,prefix_choices=$4::jsonb,reflection_prompt_text=$5,source_metadata=coalesce(source_metadata,'{}'::jsonb)||$6::jsonb,source_name=$7,source_use_note=$8,production_enabled=false,review_status='approved_for_first_exposure',reviewed_by='Katie Sanderson',reviewed_at=now() where id=$9",[batchId,JSON.stringify(input.correction.profile.introContent),JSON.stringify(input.correction.profile.meaningBins),JSON.stringify(choices),input.correction.profile.reflection,JSON.stringify({sub_inter_super_correction:source}),"Dynamic Prefix v2 SUB/INTER/SUPER reviewed staging correction","Profile examples, prefix-meaning sort and meaning-led build; reviewed word data retained unchanged.",profileId]);if(update.rowCount!==1)fail("Profile update failed.");const verify=await client.query("select production_enabled,intro_content,meaning_bins,count(*) member_count from canonical_teaching_dictionary_prefix_profiles p join canonical_teaching_dictionary_prefix_members m on m.prefix_profile_id=p.id where p.id=$1 group by production_enabled,intro_content,meaning_bins",[profileId]);const after=await client.query("select (select count(*) from adle_learning_items) learning_items,(select count(*) from daily_assignments) assignments,(select count(*) from adle_assignment_attempt_events) evidence,(select count(*) from adle_review_schedule_words) scheduling");const introMatches=verify.rows[0]?.intro_content?.title===input.correction.profile.introContent.title&&JSON.stringify(verify.rows[0]?.intro_content?.paragraphs)===JSON.stringify(input.correction.profile.introContent.paragraphs)&&Array.isArray(verify.rows[0]?.intro_content?.examples)&&verify.rows[0].intro_content.examples.every((example:any,index:number)=>example.prefix===input.correction.profile.introContent.examples[index]?.prefix&&example.base===input.correction.profile.introContent.examples[index]?.base&&example.word===input.correction.profile.introContent.examples[index]?.word&&example.meaning===input.correction.profile.introContent.examples[index]?.meaning);const binsMatch=Array.isArray(verify.rows[0]?.meaning_bins)&&verify.rows[0].meaning_bins.length===input.correction.profile.meaningBins.length&&verify.rows[0].meaning_bins.every((bin:any,index:number)=>bin.id===input.correction.profile.meaningBins[index].id&&bin.label===input.correction.profile.meaningBins[index].label&&bin.description===input.correction.profile.meaningBins[index].description);if(verify.rowCount!==1||verify.rows[0].production_enabled||+verify.rows[0].member_count!==7||!introMatches||!binsMatch||JSON.stringify(before.rows[0])!==JSON.stringify(after.rows[0]))fail("Post-correction verification failed.");await client.query("commit");console.log(JSON.stringify({status:"applied_and_verified",batchId,correctionSha256:input.sha,updated:{canonicalWords:0,metadata:0,profile:1,members:0},productionEnabled:false,prohibitedWrites:before.rows[0]}));}catch(error){await client.query("rollback");throw error}finally{await client.end()}}
async function main(){const input=await load();if(process.argv.includes("--validate")){console.log(JSON.stringify({packagePath:PACKAGE_PATH,correctionSha256:input.sha,profileKey:PROFILE_KEY,blockers:input.blockers},null,2));if(input.blockers.length)process.exitCode=1;return}if(!process.argv.includes("--apply")||arg("--environment")!=="staging"||arg("--confirm-package-sha256")!==input.sha)fail("Use --apply --environment staging --confirm-package-sha256 <exact>.");if(input.blockers.length)fail(input.blockers.join("; "));const databaseUrl=arg("--database-url");if(!databaseUrl)fail("--database-url is required.");await apply(input,databaseUrl)}main().catch((error)=>{console.error(error instanceof Error?error.message:error);process.exitCode=1});
