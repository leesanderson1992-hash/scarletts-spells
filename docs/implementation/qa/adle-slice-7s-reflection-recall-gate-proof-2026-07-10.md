# ADLE Slice 7S Reflection Recall-Gate Proof - 2026-07-10

Status: **PASSED - 7S live/manual proof recorded 2026-07-10**

## Environment

- App commit before proof documentation updates: `d9b5bd04ef79d177ea43580ada6675b60199329b`
- Local/live proof environment: local Next dev server, local Supabase proof stack at `127.0.0.1:54321`
- Browser path: visible installed Chrome driven through a temporary Playwright runner outside the repository
- Temporary proof runner dependency: `/tmp/adle-playwright-*`; not committed
- Required local DB prerequisite applied before proof: existing migration `20260709170000_add_adle_assignment_attempt_events.sql`
- Migration ledger verification: `20260709170000 | add_adle_assignment_attempt_events`

## Test Identity

- Parent/test login: `adle-parent@example.test`
- Child/test identity: `Test Scarlett`
- Child id: `e156d8c7-8b6d-4d37-8bd9-89945b3fc238`
- Assignment id: `3195fe13-d595-4c3c-9c5d-983d5966af42`
- Assignment date: `2026-07-10`

This is the repository's established disposable ADLE QA child/login from the
Slice 6 manual QA seed.

## Starting State

- `daily_assignments.status`: `pending`
- `assignment_items`: 26 `ready`
- `adle_assignment_attempt_events` for assignment: 0
- `adle_authentic_use_events` for child: 0
- `child_word_treasure_events` for child: 0

The assignment was generated by the existing guarded pilot command and not by
manual row edits.

## Acceptance Checklist

Source of truth: `docs/implementation/adle-slice-7s-reflection-recall-gate.md`,
`components/adle/activities/reflection-activity.tsx`, and
`lib/adle/assignment-attempt-events.ts`.

- A scheduled-review miss produces an `ERROR_REFLECTION_CUE` card.
- The reflection card begins in answer-visible teaching state: child attempt,
  correct spelling, and memory cue.
- The retry input is unavailable while the correct spelling is visible.
- The child-facing `Hide Word` control is present.
- The `Hide Word` control has accessible state via `aria-label="Hide Word"`.
- Activating the recall gate replaces the target with `hidden for retry`.
- The retry input appears only after the answer is hidden.
- The retry submits through `reflectionAttempts` keyed by `assignment_item_id`.
- The retry persists as `attempt_kind = reflection_retry`.
- The retry persists as `evidence_class = reflection_attempt`.
- The retry has `is_correct = null`.
- The retry does not create scheduled-review outcome evidence.
- The retry does not create authentic-use evidence.
- The retry does not create reward events.
- The session remains completable after Part 1 review submission.
- Refresh before submission restores a safe pre-submission state and does not
  expose retry entry while the answer is visible.

## Child-Facing Flow

1. Opened `/learn/week/adle?mode=child&child=e156d8c7-8b6d-4d37-8bd9-89945b3fc238`
   in the normal installed Chrome browser with the QA login session.
2. Started Part 1 review from the quick-sort screen.
3. Entered production attempts:
   - `baby` -> `bby`
   - `back` -> `back`
4. Advanced to reflection.
5. Observed reflection card:
   - displayed `You wrote bby`
   - displayed the answer-visible wording `the word is baby`
   - displayed `Hide Word`
   - did not expose a visible retry input before the recall gate
6. Refreshed before submission.
7. Confirmed the page returned to the safe review start state and did not
   preserve a bypassed retry box.
8. Re-entered the same production attempts and returned to reflection.
9. Activated `Hide Word`.
10. Observed `hidden for retry`.
11. Entered retry response `baby`.
12. Submitted `Finish Part 1`.
13. Observed the session continue with `Review finished. Nice work.` and Part 2
    available.

## Persisted Attempt Rows

`adle_assignment_attempt_events` for assignment
`3195fe13-d595-4c3c-9c5d-983d5966af42`:

| Section | Target | Attempt | Attempt kind | Evidence class | Correct |
|---|---|---|---|---|---|
| `review_production` | `baby` | `bby` | `review_production` | `scheduled_review_attempt` | `false` |
| `review_production` | `back` | `back` | `review_production` | `scheduled_review_attempt` | `true` |
| `review_reflection` | `baby` | `baby` | `reflection_retry` | `reflection_attempt` | `null` |

Reflection retry source ref:

```text
review:e156d8c7-8b6d-4d37-8bd9-89945b3fc238:2026-07-10:reflection:17e78d88-413f-4fa2-9c29-85dc572e615b
```

## Expected Writes Observed

Review outcome rows created on `2026-07-10`:

- `baby`: `retest_fail`, attempt text `bby`
- `back`: `retest_pass`, attempt text `back`

These rows correspond to the original review-production attempts, not the
reflection retry.

Part 1 item status after submission:

- 5 `completed`
- 21 `ready`

This confirms Part 1 completed and Part 2 remains available; the assignment
header remains `pending` until the lesson part is completed.

## Prohibited Writes Checked

- `adle_authentic_use_events` for child: 0
- `child_word_treasure_events` for child: 0
- No review-outcome row was created from the reflection retry.
- No reward event was created by review/reflection completion.

## Automated Validation

Before the live/browser proof:

- `npm run adle:reflection-recall-gate-regression` - passed
- `npm run adle:attempt-capture-regression` - passed
- `npm run adle:session-wiring-regression` - passed
- `npm run adle:reward-bridge-regression` - passed

After the live/browser proof:

- `npm run adle:reflection-recall-gate-regression` - passed
- `npm run adle:attempt-capture-regression` - passed
- `npm run adle:session-wiring-regression` - passed
- `npm run adle:reward-bridge-regression` - passed

## Conclusion

The 7S reflection recall gate is live/manual proven for the existing ADLE child
session implementation. The retry starts from an answer-visible teaching state,
requires the child to hide the answer before retry entry, persists as
reflection-only evidence, and does not create prohibited authentic-use,
scheduled-review, or reward semantics.

The 7S proof blocker for evidence-sensitive runtime redesign is closed.

Non-blocking observation: the proof environment first required applying the
already-existing 7R attempt-ledger migration to the local/live proof database;
the source migration itself was unchanged.
