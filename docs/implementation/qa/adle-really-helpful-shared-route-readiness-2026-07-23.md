# ADLE `really` / `helpful` shared-route readiness follow-up — 2026-07-23## Recorded stateThe canonical-target/shared-route release is live for the two tagged`playing` Test Scarlett mappings. Parent Insights now exposes active,unresolved ADLE routes in the parent-only pending queue. Neither changecreated teaching-dictionary content or imported the 1,000-word review batch.The Base Word Lab remains deliberately fail-closed. It requires two distinct,verified authentic targets for a catalogued micro-skill and exact approvedroute content for every target. The `playing` proof routes cannot satisfy thatcontract: `playing` has no runtime-approved support for either test route;the frozen source package contains an identify-base analysis only, and nopreserve-base analysis.## Replacement proof identityThe staging-only readiness proof replaces the active test route with theseexact normalised spelling relationships:| Learner spelling | Canonical target | Catalogued route || --- | --- | --- || `reelly` | `really` | `D4_MOR_BASE_WORDS_PRESERVE_BASE` || `realy` | `really` | `D4_MOR_BASE_WORDS_IDENTIFY_BASE` || `helful` | `helpful` | `D4_MOR_BASE_WORDS_PRESERVE_BASE` || `halpful` | `helpful` | `D4_MOR_BASE_WORDS_IDENTIFY_BASE` |This makes each Base Word Lab lesson contain exactly two authentic targets(`really`, `helpful`) followed by four approved transfer words. The twomicro-skills remain separate error routes; a canonical target never owns amicro-skill.## Content authority and gateThe existing frozen D4 morphology package covers `really` for preserve-baseand `helpful` for identify-base. The cross-route analyses (`really` foridentify-base and `helpful` for preserve-base) are new authored content.They are held in the companion review packet and must receive a named humansign-off before the guarded staging proof permits an import.The proof may only create disposable staging records. It must not create adictionary entry for any misspelling, mutate production, or activate the1,000-word `in_review` batch. Existing `playing` mapping and learner recordsare superseded for the test route rather than deleted, retaining their audithistory and any downstream learner evidence.## Completion gateBefore any production replacement, the staging proof must show:1. idempotent intake creates four distinct learning items and immutable   spelling lineage rows;2. both two-target lessons compose with authentic targets first;3. completing the second route creates shared next-day schedules for both   canonical targets, preserving historic schedules;4. one shared review attempt is attributed to both routes without duplicate   evidence, rewards, currency, breadth, or Word Treasure credit; and5. cleanup restores the protected staging baseline with no proof residue.