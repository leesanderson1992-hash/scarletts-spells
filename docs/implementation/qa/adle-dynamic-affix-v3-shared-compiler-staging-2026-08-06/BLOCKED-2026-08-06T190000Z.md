# Dynamic Affix V3 shared compiler staging proof — BLOCKED

Recorded at `2026-08-06T19:00:00Z` (`2026-08-06T20:00:00+01:00`).

## Exact gate

The exact audited pre-migration staging application completed a shared-created,
four-authentic Dynamic Affix V3 assignment, but persisted four active authentic
schedule rows and zero `adle_review_schedule_word_routes` rows. Four route rows
were required. The proof stopped on:

```text
rollback_four_authentic: authentic-only schedule routes

0 !== 4
```

The public V3 payload remained byte-compatible: the older application loaded,
resumed, and completed the assignment. The failure is completion storage, not a
V3 reader or payload mismatch.

At the audited baseline,
`lib/adle/loaders/session-completion-loader.ts` gates shared route persistence on
`ADLE_CANONICAL_INTAKE_ENABLED=enabled`. Read-only Vercel inspection after the
proof showed that variable absent from the verified staging project's current
Production environment list. The immutable older deployment therefore cannot
write the required links. Adding that variable and rebuilding would not be the
exact older deployment; manually adding route rows would be a prohibited
backfill. Neither action was taken.

## Identities

- Audited baseline and synchronized `main` / `origin/main`:
  `ec704af947f77a031a39dd27d8d1e27732609250` (`0 / 0`).
- Branch: `codex/dynamic-affix-v3-shared-compiler-migration`.
- Runtime implementation SHA: `1616a30fa9d4db8214a9600decb7203d9dd9a519`.
- Proof-tooling head at cleanup: `93efa57545aed70358f91ec0a9a06fc96163d456`.
- Staging Vercel: `scarletts-spells-staged` /
  `prj_oJkffstOtacc4juYloXajHpjJUha`.
- Staging Supabase: `jlhotktspjvffslvuyfz`.
- Exact older staging deployment: `dpl_4Mg6iAhv4GaESFosYnzjAQFo8g3n` /
  `scarletts-spells-staged-cm2cpvdm2.vercel.app`.
- Restored shared deployment: `dpl_6Jw7iHm47E9Pyy1wKPLLWiUfgXw3` /
  `scarletts-spells-staged-pid4arocg.vercel.app`.
- Stable staging alias was forward-restored to the shared deployment before
  cleanup.

## Authority-mode proof

| Mode | Deployment | Result |
|---|---|---|
| `legacy_authoritative` | `dpl_GtpjCJW98hUsRNaN5qu4rz38XgqV` | exact V3/plan/binding parity; legacy invoked |
| `shadow` | `dpl_5aeXuih7isAxaskWx6xHBzynyBwM` | exact parity; zero shadow writes |
| `enforced_parity` | `dpl_6BXurBbnaYykSE37tDwWLbixAqE1` | exact parity; injected public-byte mismatch blocked with zero writes |
| `shared_authoritative` | `dpl_6Jw7iHm47E9Pyy1wKPLLWiUfgXw3` | exact parity; zero legacy calls |

Each mode completed a genuine normal-writer assignment with 14 attempt events,
four Cover Check and four Dictation attempts, four taught/evidence rows, one
authentic learning item, one authentic schedule and route, one bundle, one
Reflection, four Forge rewards, and per-word evidence `0.75` / state `active` /
breadth `0.1`. Transfer words received evidence and rewards but no learning
item, schedule, or route.

## Transformation-class proof

All seven shared-authoritative normal-writer classes completed through the real
learner UI with wrong Cleaver feedback/disclosure, Cover Check, Dictation,
reload/resume, Reflection reload/resume, and durable completion:

| Class | Profile | Items | Public V3 fingerprint |
|---|---|---:|---|
| `direct_one_form` | MENT | 16 | `1589ea660288f88eb63c33ba709097cc352065586a26c083d85529dc908b8528` |
| `changed_one_form` | OUS | 16 | `7f015ba11ddd96c9771172d46ca8387b03f31b8ec9b11513933e0e8102c9b00a` |
| `replace_remove` | ITY | 16 | `e798ca600f5bad6dc7751ce93db179f34e325de72d9b209d5643864d9d31e8e1` |
| `two_form` | ABLE/IBLE | 16 | `679fd80b16371f13343ef4715325818fb38dbff77b09e6baaa2d852aeb2b5edd` |
| `two_form_meaning` | FUL/LESS | 18 | `4361050b713b304d6ebd3a09e2375f90b8fb5f4c3d3131eb16c1212679e16f74` |
| `visible_tion` | TION | 16 | `723ded2a29c121e90b65707c9d57d82f346d482506732c3a529c2b1461a1179b` |
| `visible_sion` | SION | 16 | `7c9205c24947866f7edc18119ddf0af43dcf2ff13646d770f1148c42f180043b` |

Every class produced four evidence rows and exactly one authentic
learning-item/schedule/route. FUL/LESS produced 16 attempt events because its
meaning sort contributes two additional guided bindings; the other classes
produced 14. Every class produced one Reflection and four reward events.

## Rollback and forward restoration

- Older app: shared-created four-authentic V3 loaded and completed, then failed
  the route-link assertion (`4` schedules, `0` routes).
- Older app: transfer-bearing NESS V3 loaded, retained a Cleaver miss across
  reload/resume, stayed uncompleted (`pending`), and wrote zero attempt rows.
- Forward restoration: promoted the exact shared deployment.
- Restored app: the same transfer-bearing assignment resumed and completed with
  14 attempts, four taught/evidence rows, one authentic schedule/route, no
  transfer schedule, one Reflection, and four rewards.

## Automated verification

Passing commands included TypeScript app and script type checks, ESLint,
Next.js 16.2.12 production build, 33 semantic production regressions, shared
compiler/profile/compatibility/route/completion/reward regressions,
architecture/documentation generation checks, Cover Shutter Playwright (3
passed / 3 expected skips), Dynamic Affix V3 Playwright (1 passed), and the
compiler benchmark. Authority coverage was 10 profiles, 640 ordered authentic
selections, 2,560 mode decisions, and 266 V3 mutations. Benchmark maximums were
`p95=4.823ms`, `p99=7.484ms`, and `0.017MB` heap delta.

## Cleanup and mutation boundary

Cleanup returned all protected staging counts to the recorded baseline and
reported zero residue in children, assignments, assignment items, learning
items, attempts, reflections, taught history, bundles, schedules, schedule
routes, treasures, reward events, and auth users. The 10-profile / 40-member
projection remained unchanged at fingerprint
`43ddc766593a2adb1b8f14eee82ea47d5e769eb18f1f0b8e06674542312a633f`.

Production, Dynamic Prefix, canonical-intake data/backlog, Common Word Lab,
Generic Snapshot, Closed Compound, Base Word, and `origin/main` were not
mutated.

