# Dynamic Prefix staging enrichment candidate

Generated by `scripts/build-d4-dynamic-prefix-staging-enrichment.py`.

This package is staging-only candidate evidence. It creates no database rows,
learning items, assignments, evidence, scheduler state, or production enablement.
British IPA is the primary pronunciation source; CMUdict is comparison/fallback
evidence. A missing or unsafe pronunciation result remains `in_review`, and the
package remains blocked until every required field is approved.

`complexityLevel` is a structural preview (`banding_v1.1_2026-07-04.preview`),
not a canonical complexity band or an activation override.
