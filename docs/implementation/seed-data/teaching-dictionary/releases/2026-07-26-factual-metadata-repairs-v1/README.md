# Superseded before release

This immutable package was prepared but never applied to staging or production.
Its staging preflight correctly blocked because `govern_en_gb` already had
active metadata. A read-only reconciliation also confirmed that
`governor_en_gb` already had active metadata.

Do not release this package. The valid successor is
`2026-07-26-tall-factual-metadata-repair-v2`, which contains only the genuinely
missing `tall_en_gb` metadata repair and has a verified staging receipt.
